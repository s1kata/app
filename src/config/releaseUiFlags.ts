/**
 * Релизный флаг: скрыть UI следующего патча (блок настроек уведомлений / пуши в настройках).
 * Отели и лишние экраны убраны из AppNavigator (возврат в следующих релизах).
 * Для ветки next-patch: выставить false и вернуть экраны в навигатор (см. комментарии в AppNavigator).
 */
export const RELEASE_HIDE_NEXT_PATCH_UI = true;
